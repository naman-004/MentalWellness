# ZenPath Local Environment Variables Template

# Google Gemini API Key (Used to analyze wellness logs and power the chat companion)
# ZenPath reads this to prepopulate or configure the client SDK. At runtime, 
# the user stores their key in local storage.
VITE_GEMINI_API_KEY=your_gemini_api_key_here
